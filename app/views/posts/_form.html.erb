<%= form_with model: @post, url:user_posts_path, method: :post, local: true, scope: :post do |f| %>
  <div>
    <%= f.label :Title %><br>
    <%= f.text_field :Title %>
  </div>
  <div>
    <%= f.label :Text %><br>
    <%= f.text_field :Text %>
  </div>
   <%= f.submit "Add Post" %>
<% end %>